{
  "name": "calibre-warehouse-mobile",
  "version": "1.0.0",
  "description": "Calibre Auto Parts — Warehouse Hub Android app (native shell around the live web app)",
  "author": "Calibre Auto Parts Ltd",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "add:android": "cap add android",
    "sync": "cap sync android",
    "open": "cap open android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
